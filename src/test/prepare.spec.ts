/**
 * Need to find a better way of doing this, but for now this is the best way.
 */

describe("Starting", () => {
    it("It should wait until the server is ready", (done) => {
        setTimeout(done, 2000);
    });
});